# ===================================================================
# Semantic Map Perception Parameters
# ===================================================================

perception_collector_node:
  ros__parameters:
    camera_topic: "/head_front_camera/color/image_raw"
    capture_delay_sec: 1.0

ocr_node:
  ros__parameters:
    confidence_threshold: 0.4
    languages: ["en", "es"]

vlm_labeler_node:
  ros__parameters:
    # Ollama (local, default)
    api_url: "http://172.17.0.1:11434/api/chat"
    model_name: "qwen3-vl:4b"
    api_key: ""
    timeout_sec: 60.0
    use_openai_format: false
